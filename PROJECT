-- Project Lighthouse

-- The main idea of the project is to create a database where we can store our tables
-- We need to have 3 tables named customers, orders and products

-- 1.1 First, we create a database in MySQL and select it

CREATE DATABASE lighthouse_db;
-- It tells to MySQL to create a new place where tables and data will be stored.

USE lighthouse_db;
-- It selects the database you want to work with.
-- After this command, all tables you create will be added to that database.

-- 1.2 Create the first table: customers

CREATE TABLE customers(
    customer_id INT PRIMARY KEY,
    customer_name VARCHAR(100),
    city VARCHAR(100)
);

-- CREATE - creates an object in MySQL
-- TABLE - specifies that the object is a table
-- customers - name of the table
-- ( - starts column definitions
-- customer_id - column storing customer ID
-- INT - integer (whole number)
-- PRIMARY KEY - uniquely identifies each row (must be unique and not NULL)
-- customer_name - column for customer name
-- VARCHAR(100) - text up to 100 characters
-- city - column for city
-- ) - ends table definition
-- ; - ends the SQL command

-- 1.3 Create the second table: products

CREATE TABLE products (
    product_id INT PRIMARY KEY,
    product_name VARCHAR(100),
    price DECIMAL(10,2)
);

-- product_id - unique identifier for each product
-- INT - integer (whole number)
-- PRIMARY KEY - uniquely identifies each product
-- product_name - name of the product (text)
-- VARCHAR(100) - variable-length text up to 100 characters
-- price - stores product price
-- DECIMAL(10,2) - precise decimal number:
-- 10 = total digits allowed
-- 2 = digits after decimal point (used for money)

-- 1.4 Create the third table: orders

CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    customer_id INT,
    product_id INT,
    quantity INT,

    FOREIGN KEY (customer_id)
    REFERENCES customers(customer_id),

    FOREIGN KEY (product_id)
    REFERENCES products(product_id)
);

-- order_id - unique identifier for each order
-- INT - integer (whole number)
-- PRIMARY KEY - uniquely identifies each order

-- customer_id - links order to a customer
-- INT - integer (whole number)

-- product_id - links order to a product
-- INT - integer (whole number)

-- quantity - number of products ordered
-- INT - integer (whole number)

-- FOREIGN KEY (customer_id)
-- creates a relationship between orders and customers table
-- ensures the customer exists in the customers table

-- REFERENCES customers(customer_id)
-- connects customer_id in orders to customer_id in customers

-- FOREIGN KEY (product_id)
-- creates a relationship between orders and products table

-- REFERENCES products(product_id)
-- connects product_id in orders to product_id in products

-- CREATE - It tells MySQL to create an object
-- table - Specifies that the object being created is a table
-- customers - This table will store customer information / the name of the table
-- ( - This is where the column definitions begin
-- customer_id - This column stores the customer ID
-- INT - (Integer) - A numeric data type for whole numbers
-- PRIMARY KEY - It uniquely identifies each row. it has two rules:
--                      - every value must be unique
--                      - cannot be NULL



-- PRIMARY KEY & FOREIGN KEY

-- PRIMARY KEY - is a column (or group of columns) that uniquely identifies each row in a table
-- every customer has a unique ID
-- no two customers can have the same customer_id for example
-- in simple ideea PRIMARY KEY is the 'ID card' of a table

-- FOREIGN KEY is a column that creates a link between two tables
-- for example the customer_id in our table must exist in the customers table
-- It points to a Primary KEY in another table.
-- in simple ideea FOREIGN KEY a “reference link” to another table

-- KEY diference:
-- PRIMARY KEY → identifies a row inside its own table
-- Foreign KEY → connects one table to another table
-- Primary Key uniquely identifies records, while Foreign Key creates relationships between tables

-- 1.5 now we need to insert our data in the 1st values

INSERT INTO customers VALUES
(1, 'Alice', 'London'),
(2, 'Bob', 'Paris');

-- INSERT - to add data
-- INTO - it specifies where the data will go
-- customers - this is the table where you inserting data
-- VALUES - It introduces the actual rows we want to insert
-- (1, 'Alice', 'London') first row
-- 1 - customer_id (number, unique ID) which is replicated in orders table
-- 'Alice' - customer_name (text/string)
-- 'London' - city (text/string)



INSERT INTO products VALUES
(1, 'Laptop', 1200.00),
(2, 'Mouse', 25.50);

-- INSERT - to add data
-- INTO - it specifies where the data will go
-- products - this is the table where you inserting data
-- VALUES - It introduces the actual rows we want to insert
-- 1 - product_id (unique identifier for the product)
-- 'Laptop' - product_name (text/string)
-- 1200.00 - price (decimal value, money format)


INSERT INTO orders VALUES
(1, 1, 1, 1),
(2, 2, 2, 3);

-- INSERT - to add data
-- INTO - it specifies where the data will go
-- orders - this is the table where you inserting data
-- 1 → order_id (unique ID for the order)
-- 1 → customer_id (links to customers table)
-- 1 → product_id (links to products table)
-- 1 → quantity (how many products were ordered)

-- Meaning Customer 1 (Alice) ordered 1 Laptop
